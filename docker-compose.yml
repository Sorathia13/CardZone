version: '3'

services:
  # Service pour le backend
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb+srv://kimi:kimikimikimi@cluster0.9vmkx.mongodb.net/cardzonedb?retryWrites=true&w=majority
      - JWT_SECRET=cestunedingueriedefaireca
      - PORT=5000
    restart: unless-stopped

  # Service pour le frontend (à décommenter quand vous êtes prêt à l'ajouter)
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - backend
  #   environment:
  #     - REACT_APP_API_URL=http://backend:5000
  #   restart: unless-stopped
